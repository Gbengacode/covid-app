import React from 'react';
import { HeaderContainer, HeaderTitle } from '../../assets/styles/navStyles';

const Header = () => (
  <>
    <HeaderContainer>
      <HeaderTitle>
        Covid Stats App
      </HeaderTitle>
    </HeaderContainer>

  </>
);

export default Header;
